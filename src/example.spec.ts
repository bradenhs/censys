// Project doesn't have complex enough unit-level logic to really make
// unit tests worth it so this is just a placeholder. The complexity
// level however is high enough at the UI level to merit some UI tests.
// See those test in the cypress/integration directory. While running
// "npm start" open a new terminal and run "npm run cypress" to see these
// tests in action.
describe("example", () => {
  it("should work", () => {
    expect(true).toBe(true);
  });
});
